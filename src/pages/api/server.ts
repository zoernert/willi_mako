// Single-Port Development Setup
// This file is not needed - we use next.config.js rewrites instead
export default function handler() {
  return { message: 'API routes are handled via next.config.js rewrites to Express server' };
}
